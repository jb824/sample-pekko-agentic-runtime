
- Each agent has its own SYSTEM MESSAGE
## Tools
- API for building tools should be more intuitive. For example, akka has 
4 ways to add function tools to agent that use the @FunctionTool decorator 
and each approach is less verbose than current implementation. 
      1. **Agent-defined function tools** — Define function tools directly within your agent class using the `@FunctionTool` annotation. These are automatically registered as available tools for the current Agent.
      2. **Externally defined function tools** — Explicitly register external objects or classes containing function tools by
         passing them to the `effects().tools()` method in your agent’s command handler. Objects or classes passed to `effects
      ().tools()` must have at least one public method annotated with `@FunctionTool`.
      3. **Akka components as function tools** — Use Akka components from the same application as tools by annotating their command handlers with `@FunctionTool` and passing the component class to the `effects().tools()` method. This approach works with Event Sourced Entities, Key Value Entities, Workflows, and Views.
      4. **Tools defined by remote MCP servers** – Register remote MCP servers to let the agent use tools they provide.
- For example, a tool that get's current date should be a simple function like: 
```java
    @FunctionTool(description = "Return current date in yyyy-MM-dd format")
    private String getCurrentDate() {
        return LocalDateTime.now().format(DateTimeFormatter.ISO_LOCAL_DATE);
    }
```


## Client (OpenAI, Bedrock, vLLM, ollama, llama.cpp)
- LLM client can be defined in resources/application.conf on client side 


## Tasks
The correct approach is to think of GatewayAgent `uses` Tasks. The GatewayAgent
is the planner/coordinator/supervisor of a task, and it oversees the state of 
the task, ensuring the task is completed until the end. Right now, each agent 
`uses` each task which results in many several tasks being completed
in a single run. 

## Endpoints 
We need to have better API for declaring RESTful endpoints + gRPC