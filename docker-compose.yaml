services:
  cassandra:
    image: cassandra:5
    container_name: cassandra-dev
    ports:
      - "127.0.0.1:9042:9042"
    environment:
      CASSANDRA_CLUSTER_NAME: dev-cluster
      CASSANDRA_DC: datacenter1
      CASSANDRA_ENDPOINT_SNITCH: GossipingPropertyFileSnitch
    volumes:
      - cassandra-dev-data:/var/lib/cassandra

volumes:
  cassandra-dev-data:
